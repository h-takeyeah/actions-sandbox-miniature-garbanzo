name: nodejs-yarn

on:
  workflow_dispatch:

jobs:
  install_build:
    name: Install / Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: yarn

      - name: Install dependencies
        run: yarn
      # TODO: build
