name: Print env

permissions: {}

on:
  workflow_dispatch:

jobs:
  env:
    runs-on: self-hosted

    steps:
      - name: print env
        run: env
      - name: cat .bashrc
        run: cat ~/.bashrc
